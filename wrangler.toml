# Cloudflare Workers configuration - Updated
name = "bookstore"
main = "workers-site/index.js"
compatibility_date = "2025-12-29"

[site]
bucket = "./workers-site/dist"

[build]
command = "cd client && npm run build && mv build ../workers-site/dist"
